#include <stdio.h>
#include <stdlib.h>

struct tag{
  int val;
  struct tag* next;
};

int main(){
  struct tag *p, *q = NULL, *start;

  int n, i;
  printf("Enter the number of nodes: ");
  scanf("%d", &n);

  for(i = 0; i < n; i++){
    
  }
}